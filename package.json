{
    "name": "wasmx-monorepo",
    "private": true,
    "workspaces": [
      "packages/*"
    ],
    "overrides": {

    },
    "scripts": {
      "install-all": "npm install",
      "reinstall-all": "rm -rf node_modules packages/*/node_modules package-lock.json && npm install"
    }
}
